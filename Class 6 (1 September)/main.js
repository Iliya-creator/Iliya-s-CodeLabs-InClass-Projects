// Create a function that will take one number parameter and count to that number by outputting zero to that number.

function countTo(num){
    for (let i = 0; i <= num; i++){
        console.log(i);
    }
}

countTo(1234);